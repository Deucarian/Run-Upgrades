# Run Upgrade Draft

Open `RunUpgradeDraft.unity` and enter Play mode. The sample builds one additive-damage upgrade, creates a deterministic one-choice draft, and selects the result. Inspect the `Selected Rank` property on the sample component after startup; it becomes `1`.

The effect descriptor remains data only. Applying it to an attack or weapon is deliberately left to a game-owned adapter.
